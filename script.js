// Función para abrir y cerrar el panel lateral
function toggleSidebar() {
  var sidebar = document.getElementById("sidebar");
  sidebar.classList.toggle("active"); // Alterna la clase 'active' para mostrar u ocultar el sidebar
}
